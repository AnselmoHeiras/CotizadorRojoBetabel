<UserControl x:Class="CotizadorRojoBetabel.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CotizadorRojoBetabel.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border CornerRadius="40">
        <Border.Background>
            <SolidColorBrush Color="White" Opacity="0.8"/>
        </Border.Background>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Name="IndicationsTxt" Text="Para iniciar seleccione una opción del menú lateral" FontSize="32" TextWrapping="Wrap" Grid.Row="1" VerticalAlignment="Center" TextAlignment="Center" Margin="50,0,50,0" Foreground="{DynamicResource RBGrayForeground}"></TextBlock>

        </Grid>
    </Border>
</UserControl>
